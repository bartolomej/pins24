fun main() =
    putstr(^hello),

    # Should this work as well?
    # (hello + 2)^ = 65,

    # This assigns the address of the right string to variable hello
    # hello = "Bye\n\00",

    hello = 'B',
    (^hello + 4)^ = 'y',
    (^hello + 8)^ = 'e',
    (^hello + 12)^ = '\n',
    (^hello + 16)^ = '\00',
    putstr(^hello)


var hello = "Hello\n\00"

fun exit(exitcode)
fun getint()
fun putint(value)
fun getstr(straddr)
fun putstr(straddr)
fun new(size)
fun del(straddr)
